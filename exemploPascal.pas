program exemplo(input, output);
var x, y : integer ;
function mdc (a , b : integer) : integer; 
begin 
	begin
		
		read(x, y);
		begin
			read(x, y);
			write(mdc(x,y))
		end
		
	end;
	if b =0 then
		mdc := a + b
	else 
		mdc :=mdc(b, a mod b);
	  mdc :=mdc(b, a mod b);
	   mdc :=mdc(b, a mod b)
end;
begin
	begin
			read(x, y);
			write(mdc(x,y))
		end;
	read(x, y);
	write(mdc(x,y))
end.
